import { GetStringLengthPipe } from './get-string-length-pipe';

describe('GetStringLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new GetStringLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
